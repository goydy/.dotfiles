[module/memory]
type=internal/memory

interval=5

format=<label>
format-font=14
format-prefix="%{A1:xfce4-terminal -e btop &:}  %{A}"
format-foreground=${color.yellow}
format-background=${color.altblend}

label-font=1
label="%{A1:xfce4-terminal -e btop &:} %gb_used%%{A}"